#!/usr/bin/env bash

set -eo pipefail

if [[ -n "${DEBUG}" ]]; then
  set -x
fi

_gotpl() {
  if [[ -f "/etc/gotpl/$1" ]]; then
    gotpl "/etc/gotpl/$1" >"$2"
  fi
}

_gotpl "prometheus-init.tmpl" "/usr/local/bin/prometheus-init"
chmod +x /usr/local/bin/prometheus-init
